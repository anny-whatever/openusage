{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimum permissions for the local OpenUsage Windows tray panel.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:tray:default",
    "core:window:default"
  ]
}

